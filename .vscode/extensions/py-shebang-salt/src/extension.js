const vscode = require('vscode');

function activate(context) {
    console.log('py-shebang-salt extension is active!');
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
};